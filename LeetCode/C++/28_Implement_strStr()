class Solution {
public:
    int strStr(string haystack, string needle) {
        if(needle.length()==0)
            return 0;
        auto i=haystack.find(needle);
        return i!=string::npos?i:-1;
    }
};
